{
  "name": "vouch",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "vouch — a society simulator on the ALMA protocol. Monorepo: vouch-core, vouch-world, vouch-node, vouch-mcp, vouch-cli. This root package holds only the shared lint/format tooling (oxlint lints, Biome formats); each package is self-contained and run with Bun.",
  "license": "Apache-2.0",
  "scripts": {
    "lint": "oxlint .",
    "format": "biome format --write .",
    "check": "biome check --write . && oxlint .",
    "ci": "biome ci . && oxlint ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.0",
    "oxlint": "^1.77.0"
  }
}
