{
  "name": "vouch-web",
  "version": "0.0.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "description": "A read-only Web GUI for a vouch world — the village viewer + live event newspaper, served by a tiny Bun BFF that proxies vouch-node's read-only observation surface (Track C).",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "start": "bun src/main.ts",
    "dev": "bun --watch src/main.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.6.0",
    "vouch-core": "file:../vouch-core",
    "vouch-node": "file:../vouch-node"
  }
}
